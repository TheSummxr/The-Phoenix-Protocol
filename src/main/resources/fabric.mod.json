{
  "schemaVersion": 1,
  "id": "hardcorereset",
  "version": "${version}",
  "name": "HardcoreReset",
  "description": "Server-side mod that tracks player deaths and resets the entire world when a death threshold is reached. Uses disposable dimensions so the root overworld acts as a safe lobby.",
  "authors": ["Mitchell"],
  "contact": {},
  "license": "MIT",
  "environment": "server",
  "entrypoints": {
    "server": ["com.hardcorereset.HardcoreResetMod"]
  },
  "mixins": ["hardcorereset.mixins.json"],
  "depends": {
    "fabricloader": ">=0.16.0",
    "fabric-api": "*",
    "minecraft": "26.2",
    "java": ">=25"
  }
}
