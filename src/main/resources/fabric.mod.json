{
  "schemaVersion": 1,
  "id": "phoenixprotocol",
  "version": "${version}",
  "name": "The-Phoenix-Protocol",
  "description": "Server-side mod that tracks player deaths and resets the entire world when a death threshold is reached. Uses disposable dimensions so the root overworld acts as a safe lobby.",
  "authors": ["Mitchell"],
  "contact": {},
  "license": "MIT",
  "environment": "server",
  "entrypoints": {
    "server": ["com.phoenixprotocol.PhoenixProtocolMod"]
  },
  "mixins": ["phoenixprotocol.mixins.json"],
  "depends": {
    "fabricloader": ">=0.16.0",
    "fabric-api": "*",
    "minecraft": "26.2",
    "java": ">=25"
  }
}

